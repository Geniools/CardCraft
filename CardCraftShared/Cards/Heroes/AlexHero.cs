namespace CardCraftShared.Cards.Heroes;

internal class AlexHero : BaseHero
{
    public AlexHero(int health, string color, string image, string name) : base(health, color, image, name)
    {
    }
}
